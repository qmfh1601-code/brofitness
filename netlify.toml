# 브로피트니스 — Netlify 빌드 설정
# Netlify 가 GitHub 저장소를 받아 배포할 때마다 아래 명령으로
# 칼럼 정적페이지/사이트맵/SPA 데이터를 '그 시점 한국 날짜' 기준으로 다시 생성한다.
# → 예약발행: 발행일이 된 글만 자동으로 공개됨.
[build]
  command = "python3 tools/publish.py"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.11"
